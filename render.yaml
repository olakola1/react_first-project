services:
  - type: web
    name: recipes-backend
    runtime: node
    buildCommand: |
      cd server &&
      npm install && 
      npm run build && 
      npm prune --production
    startCommand: |
      cd server &&
      node dist/app.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: recipes_db_nzs9
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: PROD_PORT
        value: 10000